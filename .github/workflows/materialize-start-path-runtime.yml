name: Materialize Start With Bitcoin path runtime

on:
  push:
    branches:
      - engineering/learn-start-path-pilot
    paths:
      - 'scripts/generate-msc-learn-start-path-runtime.mjs'
      - 'package.json'
      - '.github/workflows/materialize-start-path-runtime.yml'

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: engineering/learn-start-path-pilot

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Generate runtime outputs
        run: npm run build:learn-path-pilot

      - name: Commit generated outputs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/learn/runtime/MSC-PATH-START.json snippets/msc-learn-start-path-runtime.liquid
          if git diff --cached --quiet; then
            echo "Generated outputs already synchronized."
            exit 0
          fi
          git commit -m "Generate Start With Bitcoin path runtime outputs"
          git push
